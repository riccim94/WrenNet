defaults:
  - base

project:
  name: birds_distillation_edge_debug

trainer:
  max_epochs: 1
  log_every_n_steps: 1

data:
  batch_size: 4
  num_workers: 0
  persistent_workers: false
  pin_memory: false
  debug_files_limit: 16

logging:
  level: DEBUG
